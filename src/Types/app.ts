export type projects = {
  data: {
    description?: string;
    homepage?: string;
    html_url?: string;
    name?: string;
  };
  description?: string;
  homepage?: string;
  html_url?: string;
  name?: string;
  id?: number;
};

export type apiRepos = {
  description?: string;
  homepage?: string;
  html_url?: string;
  name?: string;
}[]
